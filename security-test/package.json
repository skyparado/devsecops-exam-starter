{
  "name": "macky-security-test",
  "version": "1.0.0",
  "private": true,
  "description": "Deliberately vulnerable audit fixture; never deploy.",
  "devDependencies": {
    "lodash": "4.17.20"
  }
}
